import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'org.appdevleague.devnetic',
  appName: 'Devnetic',
  webDir: './app',
  bundledWebRuntime: false
};

export default config;


